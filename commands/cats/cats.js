// import modules

// define bot command
module.exports = (message) => {
	// input case: user is checking Yard Activity
	if (message.content.match(/^%(c|cats)$/i)) {

	}
	// input case: user is attempting to check the cat album
	else if (message.content.match(/^%(c|cats) album/i)) {
		
	}
	// input case: user is attempting to check information on a specific cat
	else if (message.content.match(/^%(c|cats) [a-zA-Z0-9]/i)) {

	}
}